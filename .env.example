# Copy to .env for local testing. NEVER commit a filled-in .env.
# In GitHub Actions these are repository secrets, not a file.

# WhatsApp via CallMeBot (primary channel)
CALLMEBOT_PHONE=
CALLMEBOT_APIKEY=

# Telegram (fallback channel)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
